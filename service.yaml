apiVersion: v1
kind: Service
metadata:
  name: civil-engineer-web
  namespace: civil-web
  labels:
    app: civil-engineer-web
spec:
  type: ClusterIP
  selector:
    app: civil-engineer-web
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
